"""Plots estimated trajectories from a tab_results file as generated by function 'write_results_tab' in funcs.py
"""
infile = r"F:\ss_22\bachelor_arbeit\data\otherOpenTopography\teton_conservation\tab_results_1652367480.3233106.txt"
import matplotlib.pyplot as plt
import ast
with open(infile) as f:
    line = f.readlines()

#print(line)
splitlines = [i.split(';') for i in line[1:]]
print(splitlines[:-1])

traj = [i[4] for i in splitlines[:-1]]
print(traj)

fig = plt.figure(num=1)
ax = fig.add_subplot()

colours = ['black', 'gray', 'orange', 'navy', 'cornflowerblue', 'cyan']

for i, t in enumerate(traj):
    lines = ast.literal_eval(t)
    ax.plot([lines[0][0], lines[1][0]], [lines[0][1], lines[1][1]])
    #c = int(splitlines[i][2])
    #print(c)
    #for lines in t:
        #lines = ast.literal_eval(lines)
        #ax.plot([lines[0][0], lines[1][0]], [lines[0][1], lines[1][1]])#, c=colours[c])

#for c_num in range(5):
    #ax.plot([],[],c=colours[c_num+1], label='trajectory {}'.format(c_num+1))

# Turn off tick labels
ax.set_yticklabels([])
ax.set_xticklabels([])

# plt.title('Swath nr. {}\n{} Points'.format(swath_nr, np.shape(swath)[0]))
# plt.title('Alphashape of Single Flight Swath')
plt.xlabel('X')
plt.ylabel('Y')

plt.legend(loc=2)
plt.show()